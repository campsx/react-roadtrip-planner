import { fetchAll } from './fetchAll';
import { fetchByCode } from './fetchByCode';
import { fetchByCodes } from './fetchByCodes';
import { search } from './search';

export const countriesApi = {
  fetchAll,
  fetchByCode,
  fetchByCodes,
  search,
};
